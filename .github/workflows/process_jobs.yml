name: Process HN Jobs

on:
  # Schedule updates (00:30 && 12:30 UTC daily)
  schedule: [{ cron: '0 * * * *' }]
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: { branches: ['master'] }

env:
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  SUPABASE_KEY: ${{ vars.SUPABASE_KEY }}

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2
      - uses: pnpm/action-setup@v2
        with:
          version: 8
      - uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install
      - run: pnpm turbo lint prettier
      - name: Run Who Is Hiring script
        run: node apps/registry/scripts/jobs/getLatestWhoIsHiring.js
      - name: Output first 10 lines of generated JSON
        run: head -n 10 apps/registry/scripts/jobs/output/whoIsHiring.json
      - name: Process job descriptions with OpenAI
        run: node apps/registry/scripts/jobs/gpted.js
      - name: Output first 10 lines of processed jobs
        run: head -n 10 apps/registry/scripts/jobs/output/processedJobs.json || echo "No processed jobs file found"
      - name: Upload Who Is Hiring data as artifact
        uses: actions/upload-artifact@v2
        with:
          name: who-is-hiring-data
          path: apps/registry/scripts/jobs/output/whoIsHiring.json
          retention-days: 7
      - name: Upload Processed Jobs data as artifact
        uses: actions/upload-artifact@v2
        with:
          name: processed-jobs-data
          path: apps/registry/scripts/jobs/output/processedJobs.json
          retention-days: 7
