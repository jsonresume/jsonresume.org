---
'@jsonresume/sample-data': minor
---

New package: sample JSON Resume fixtures. Exports `completeResume` (the
every-section fixture), `minimalResume` (a basics + work + education + skills
subset derived from the complete fixture), and `resumeSample`.
