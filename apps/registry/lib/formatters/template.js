// Re-export theme configuration and formatting utilities
export { THEMES } from './template/themeConfig';
export { getTheme } from './template/getTheme';
import { format } from './template/format';

export { format };

const exports = { format };

export default exports;
