/**
 * @jsonresume/core/helpers
 *
 * Discoverability re-export of the framework-free @jsonresume/utils surface
 * (date, metrics, and URL helpers). Prefer importing from @jsonresume/utils
 * directly in new code; this subpath exists so the helpers are easy to find
 * from @jsonresume/core.
 *
 * @module @jsonresume/core/helpers
 */
export * from '@jsonresume/utils';
