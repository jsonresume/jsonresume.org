/**
 * Resume Education Calculation Helpers
 *
 * Re-exported from @jsonresume/utils (framework-free). Note: calculateEducationYears
 * now uses the corrected ms-per-year formula (Wave 1 bugfix).
 *
 * @module @jsonresume/core/helpers/education
 */
export {
  calculateEducationYears,
  getHighestDegree,
} from '@jsonresume/utils/metrics';
