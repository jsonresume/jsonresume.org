# jsonresume-theme-operations-precision

## 0.2.0

### Minor Changes

- a7539af: Add markdown rendering with line break support to summary, description, and highlight fields using marked with `{ breaks: true }`. Single newlines now render as line breaks, double newlines as paragraph breaks, and inline markdown formatting (bold, italic, etc.) works across all text content fields.
