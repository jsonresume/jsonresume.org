# @jsonresume/sample-data

## 0.2.1

### Patch Changes

- 4556376: Add a README documenting the `completeResume`, `minimalResume`, and
  `resumeSample` exports with install and theme-test usage examples, so the
  npmjs.com page is no longer bare. (#421)

## 0.2.0

### Minor Changes

- 36d1759: New package: sample JSON Resume fixtures. Exports `completeResume` (the
  every-section fixture), `minimalResume` (a basics + work + education + skills
  subset derived from the complete fixture), and `resumeSample`.
